# Workspace Rules - Proyek Simatang

## Deployment Workflow
Whenever code changes are made to this project:
1. Always push the changes using clasp and immediately update all active deployment IDs so the web app URLs reflect the latest changes.
2. You can execute `./deploy.sh` in the project root to perform this automatically.
